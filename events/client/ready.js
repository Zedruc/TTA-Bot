module.exports = (_null, client, Discord) => {
    console.log('Bot is online');

    client.user.setActivity("#TTA Levels", { type: 'WATCHING' });
    // client.user.setActivity("Maintenance", { type: 'WATCHING' });
}